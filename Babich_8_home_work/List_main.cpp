#include "List.h" 
/*
int main()
{
	List<int> ls(5);
	std::cin >> ls >> ls >>ls;
	std::cout << ls;
	struct Line<int>* ptr = ls.findline(3);
	ls.DelLine(ptr);
	std::cout << ls;
//	system("pause");
}
*/